<div class="ml-10 space-x-4">
    <button href="{{ route('login') }}" class="bg-gray-500">{{ __('general.Login') }}</button>
    <button href="{{ route('register') }}" class="bg-blue-500">{{ __('general.Register') }}</button>
    <button href="{{ route('contact') }}" class="bg-green-500">{{ __('general.Contact') }}</button>
    <button href="{{ route('about') }}" class="bg-yellow-500">{{ __('general.About') }}</button>
</div>
<button href="{{ route('login') }}" class="bg-gray-500">{{ __('general.Login') }}</button>
<button href="{{ route('register') }}" class="bg-blue-500">{{ __('general.Register') }}</button>
<button href="{{ route('contact') }}" class="bg-green-500">{{ __('general.Contact') }}</button>
<button href="{{ route('about') }}" class="bg-yellow-500">{{ __('general.About') }}</button>
